kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ptf-client-api-dev-pipeline-claim
  namespace: ptf-client-api-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

